<h2><%= @cocktail.name %></h2>
<ul>
 <% @cocktail.doses.each do |dose| %>
  <li>
    <%= dose.ingredient.name %>
  </li>
  <li>
    <%= dose.description %>
  </li>
    <%= button_to "delete", dose_path(dose), method: :delete %>
<% end %>
</ul>

