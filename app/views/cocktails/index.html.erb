
<div class="home-banner">
  <h1> Mix it up</h1>
  <h2 class="text-center">Cocktails Galore, which will be your next drink? Have a look below!</h2>
</div>

<div class="container">



  <div class= "cards">
    <% @cocktails.each do |cocktail| %>
      <%= link_to cocktail_path(cocktail.id) do %>

        <div class="card">
          <h3><%=  cocktail.name %></h3>
          <% if cocktail.photo.attached? %>
            <%= cl_image_tag cocktail.photo.key, height: 300, width: 300, crop: :fit, class: "image" %>
          <% else %>
            <%= image_tag "cocktail-glass-blank.jpg", alt: "empty cocktail glass", height: 300, width: 300, crop: :fit, class: "image"  %>
          <% end %>
        </div>

      <% end %>
    <% end %>


    <h4><%= link_to "Create a new Cocktail", new_cocktail_path(@cocktail_id), class: "btn button_to" %></h4>


  </div>
</div>


